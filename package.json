{
  "name": "supercars-2",
  "version": "0.1.0",
  "description": "Browser remake of Super Cars 2",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bun run tsc && bunx --bun vite build",
    "preview": "bunx --bun vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
